<table class="table table-boldered">
  <% @husbands.each do |husband|%>
    <tr>
      <td><%= link_to(husband.title, husband_path(husband))%></td>
      <td><%= husband.created_at%></td>
    </tr>
  <% end%>
</table>
